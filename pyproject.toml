[project]
name = "subl-pkg-scanner"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "duckdb>=1.5.3",
    "httpx>=0.28.1",
    "python-dotenv>=1.2.2",
    "rich>=15.0.0",
    "typer>=0.26.7",
]

[project.scripts]
subl-pkg-scanner = "subl_pkg_scanner.cli:app"

[tool.uv]
package = true
